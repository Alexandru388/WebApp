@{
    ViewData["Title"] = "Pagina Principală";
}

<div class="text-center mt-5">
    <h1>@ViewData["Message"]</h1>
    <div class="mt-4">
        <a href="@Url.Action("Counter", "Home")" class="btn btn-secondary btn-lg m-2">
            Accesează pagina de Counter
        </a>
        <a href="@Url.Action("Login", "Administrator")" class="btn btn-primary btn-lg m-2">
            Logare Administrator
        </a>
        <a href="@Url.Action("List", "Administrator")" class="btn btn-info btn-lg m-2">
            Vezi administratorii
        </a>
        <a href="@Url.Action("Login", "Student")" class="btn btn-success btn-lg m-2">
            Logare Student
        </a>
    </div>
</div>